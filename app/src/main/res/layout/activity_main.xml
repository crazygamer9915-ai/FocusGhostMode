<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark"
    tools:context=".ui.home.MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/background_dark"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:titleTextColor="@color/white"
            app:title="@string/app_name" />

    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Ghost active indicator chip -->
        <com.google.android.material.chip.Chip
            android:id="@+id/ghostActiveIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="● GHOST MODE ACTIVE"
            android:textColor="@color/green_400"
            android:textSize="11sp"
            android:letterSpacing="0.12"
            app:chipBackgroundColor="@color/green_alpha"
            app:chipStrokeColor="@color/green_400"
            app:chipStrokeWidth="1dp"
            android:visibility="gone"
            android:layout_marginBottom="24dp" />

        <!-- Ghost icon -->
        <ImageView
            android:id="@+id/ivGhostIcon"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:src="@drawable/ic_ghost"
            android:layout_marginBottom="8dp"
            android:contentDescription="Ghost Icon" />

        <!-- App title -->
        <TextView
            android:id="@+id/tvAppTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="28sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="8dp" />

        <!-- Subtitle -->
        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/ghost_mode_subtitle_idle"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp" />

        <!-- Session timer -->
        <TextView
            android:id="@+id/tvTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textColor="@color/purple_200"
            android:textSize="48sp"
            android:fontFamily="monospace"
            android:textStyle="bold"
            android:visibility="gone"
            android:layout_marginBottom="32dp" />

        <!-- Ghost mode toggle button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGhostToggle"
            android:layout_width="280dp"
            android:layout_height="64dp"
            android:text="@string/activate_ghost_mode"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:fontFamily="sans-serif-medium"
            app:cornerRadius="32dp"
            app:backgroundTint="@color/purple_700"
            android:layout_marginTop="16dp" />

        <!-- History button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnViewHistory"
            android:layout_width="200dp"
            android:layout_height="48dp"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:text="View History"
            android:textColor="@color/purple_200"
            app:strokeColor="@color/purple_200"
            app:cornerRadius="24dp"
            android:layout_marginTop="16dp" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
